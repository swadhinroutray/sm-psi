const { hello } = require("./hello");
const { keySetup } = require("./keysetup");
const { intersectionCall } = require("./intersection");
module.exports = {
  hello,
  keySetup,
  intersectionCall,
};
