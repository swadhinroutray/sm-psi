const { hello } = require("./hello");
const { keySetup, keyUpdate } = require("./keysetup");
const { intersectionCall } = require("./intersection");
module.exports = {
  hello,
  keySetup,
  intersectionCall,
  keyUpdate,
};
